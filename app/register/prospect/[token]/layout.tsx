import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Inscription Prospect - MIRAI Services',
    description: 'Inscrivez-vous en tant que prospect pour bénéficier des services de blanchisserie MIRAI Services',
};
export default function ProspectRegisterLayout({ children }: { children: React.ReactNode }) {
    return children;
}
