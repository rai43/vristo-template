# ================================================================
# MIRAI SERVICES — Frontend (standalone, non-Docker)
# ================================================================
# Only needed if running outside Docker. Docker uses root .env.

NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
NEXT_PUBLIC_APP_NAME=MIRAI Services BO
NEXT_PUBLIC_APP_VERSION=1.0.0
NEXT_TELEMETRY_DISABLED=1
